namespace Skillworks.AppHost;

// The API tests start this image too, so the queries they prove are the queries this Tempo answers.
public static class TempoImage
{
    public const string Name = "grafana/tempo";

    public const string Tag = "2.10.8";
}
